/**
 * 一个始终返回 false 的函数。这经常被用作一个条件函数的占位符，该条件函数尚未实现。
 *
 * @since 1.0.0
 * @category Util
 * @returns {boolean} Returns `false`.
 * @example
 *
 * NO()
 * // => false
 */
export const no = () => false;
